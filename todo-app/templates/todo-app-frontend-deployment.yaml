apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-frontend
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-frontend
    spec:
      containers:
        - env:
          - name: POSTGRES_HOST
            value: {{ .Values.todoApp.configMap.postgresHost }}
          - name: POSTGRES_DB
            valueFrom:
              configMapKeyRef:
                name: {{ .Release.Name }}-configmap
                key: postgres_db
          - name: POSTGRES_USER
            valueFrom:
              secretKeyRef:
                name: {{ .Release.Name }}-secret
                key: postgres_user
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Release.Name }}-secret
                key: postgres_password
          name: todos
          image: {{ .Values.todoApp.frontend.image }}
          imagePullPolicy: Never
          ports:
            - containerPort: {{ .Values.todoApp.frontend.containerPort }}
          readinessProbe:
            httpGet:
              path: /api/todo/healthz
              port: {{ .Values.todoApp.frontend.containerPort }}
